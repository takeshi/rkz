<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions" style="margin-bottom:10px" >
    <%= f.submit :class=>"btn",:value=>"投稿"%>
  </div>

  <div class="field">
    <%= f.text_field :title ,:style=>"width:100%" ,:placeholder=>"Title" %>
  </div>
  <div class="field">
    <%= f.text_area :contents, :style=>"width:100%" ,:placeholder=>"Contents"%>
  </div>

  <div class="actions">
    <%= f.submit :class=>"btn",:value=>"投稿"%>
  </div>

<% end %>
